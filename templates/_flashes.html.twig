{# {% for label, messages in app.flashes %}
    {% for message in messages %}
        <div id="live-flash-ctn">
            <div id="liveFlash" class="flash" role="alert" aria-live="assertive" aria-atomic="true">
                <div class="flash-header {{ label }}">
                    <strong class="me-auto">Notification</strong>
                    <button type="button" class="btn-close" aria-label="Close" onclick='this.parentNode.parentNode.parentNode.style.display = "none";'>X</button>
                </div>
                <div class="flash-body">
                    {{ message }}
                </div>
            </div>
        </div>
    {% endfor %}
{% endfor %} #}

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div id="live-flash-ctn" class="{{ label }}">
            <button type="button" class="btn-close" aria-label="Close" onclick='this.parentNode.style.display = "none";'>X</button>
            <div class="flash-body">
                {{ message }}
            </div>
        </div>
    {% endfor %}
{% endfor %}