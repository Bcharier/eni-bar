{% extends 'base.html.twig' %}

{% block title %}{{ tab_name_prefix }}  Hello ProfileController!{% endblock %}
{% block stylesheets %}<link rel="stylesheet" href="{{ asset('styles/participant.css') }}">{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 85%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bonjour {{ user.getFullName() }} !</h1>

    {{ form_start(form, { 'attr' : {'enctype': 'multipart/form-data'}}) }}

    {{form_end(form)}}

</div>

    <img src="{{asset('userData/images/userProfile/')}}{{ user.getImageName()}}" alt="">

{% endblock %}